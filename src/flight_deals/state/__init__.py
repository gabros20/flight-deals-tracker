"""Durable state: atomic store helpers + append-only deal snapshots.

Task 7 creates the minimal atomic-write/append-JSONL helpers it needs here
(``store.py``) so there is exactly ONE implementation; Task 8 extends this
module (flock, versioned reads, saved searches, alert state) on top of it.
"""
